apiVersion: apps/v1
kind: Deployment
metadata:
  name: slave-0
  labels:
    role: slave-0
spec:
  replicas: 1
  selector:
    matchLabels:
      role: slave-0
  template:
    metadata:
      labels:
        role: slave-0
    spec:
      containers:
        - name: service-slave
          image: pairfy/service-slave
          envFrom:
            - configMapRef:
                name: slave-config       
            - configMapRef:
                name: redis-config                     
          env:
            - name: NODE_ENV
              value: "development"
            - name: SLAVE_NAME
              value: "slave-0"
            - name: MARKET
              value: "crypto"              
            - name: SYMBOL
              value: "BTCUSDT"
            - name: INTERVAL
              value: "4h"
            - name: SIDE
              value: "LONG"          
            - name: ACCOUNT_BALANCE
              value: "2000"               
            - name: ACCOUNT_RISK
              value: "1"                          
            - name: STOP_LOSS
              value: "4.2"
            - name: TAKE_PROFIT
              value: "6.9"              
            - name: CONTRACT_SIZE
              value: "1"
            - name: SHOW_PLOTS
              value: "false"              
            - name: DESCRIPTION
              value: "RSI,Squeeze,ADX,MFI"  
            - name: MARKET_HOST
              value: "http://service-market:8001"
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-password-secret
                  key: password
                                    
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: slave-storage
              mountPath: /app/output
      volumes:
        - name: slave-storage
          persistentVolumeClaim:
            claimName: pvc-slave-0
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-slave-0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: service-slave-0
spec:
  selector:
    role: slave-0
  ports:
    - name: http
      protocol: TCP
      port: 8002
      targetPort: 8002
