info:
  name: report-state
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/master/report-state
  body:
    type: json
    data: |-
      {
        "id": "Execution",
        "status": "Ready",
        "version": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
