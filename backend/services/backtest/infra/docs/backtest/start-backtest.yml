info:
  name: start-backtest
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3002/api/backtest/master/start-backtest
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
