info:
  name: get-state
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3002/api/backtest/master/get-state
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
