info:
  name: /get-history
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3001/api/market/get-history
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
